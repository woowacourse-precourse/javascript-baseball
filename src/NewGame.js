const { Console, Random } = require("@woowacourse/mission-utils");
class NewGame {
  constructor() {
    this.gameStart();
  }
  gameStart() {
    //무작위 숫자 받아오기
    //사용자 숫자 받아오기
  }
}

module.exports = NewGame;
