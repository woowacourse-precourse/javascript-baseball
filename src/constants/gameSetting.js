const MIN_NUMBER = 1;
const MAX_NUMBER = 9;
const VALUE_SIZE = 3;
const RESTART = '1';
const GAME_OVER = '2';
const NOTHING = '낫싱';
const BALL = '볼';
const STRIKE = '스트라이크';

module.exports = {
  MIN_NUMBER,
  MAX_NUMBER,
  VALUE_SIZE,
  RESTART,
  GAME_OVER,
  NOTHING,
  BALL,
  STRIKE,
};
