const ERROR = {
  INVALID_USER_INPUT: "Invalid User Input",
  INVALID_RESTART_INPUT: "Invalid Restart Input",
};

const RESTART = {
  YES: "1",
  NO: "2",
};

exports.ERROR = ERROR;
exports.RESTART = RESTART;
