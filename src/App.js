const MissionUtils = require("@woowacourse/mission-utils");
const {
  GAME_CONFIG_NUMBER,
  ERROR_CHECK,
  INGAME_MESSAGE,
  GAME_RESULT,
  RESPONSE,
} = require("./Constant");

class App {
  
  validation(input) {}
  typeCheck(input) {}
  lengthCheck(input) {}
  repeatCheck(input) {}

  play() {}
}

module.exports = App;
