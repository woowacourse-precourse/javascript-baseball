const MissionUtils = require("@woowacourse/mission-utils");
const {
  GAME_CONFIG_NUMBER,
  ERROR_CHECK,
  INGAME_MESSAGE,
  GAME_RESULT,
  RESPONSE,
} = require("./Constant");

class App {
  play() {}
}

module.exports = App;
