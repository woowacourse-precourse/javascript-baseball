/* eslint-disable max-lines-per-function */
const MissionUtils = require("@woowacourse/mission-utils");
let arrA = [1, 4, 3, 2];
let arrB = [5, 8, 6, 7, 9];

arrA.filter(it => arrB.includes(it))
function sfd (){

    if(arrA.filter(it => arrB.includes(it)).length === 0){
        MissionUtils.Console.print("낫싱")
    }
}
