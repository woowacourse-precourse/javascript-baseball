const Io = require('./Io');
class Game {
  constructor() {
  }

  /**
   * 커맨드를 실행한다.
   * @public 
   * @method
   * @return {} void
   * @description 게임을 시작한다. 클래스 외부에서 호출한다. 
   */
  playCommand () {}
}

module.exports = Game;
