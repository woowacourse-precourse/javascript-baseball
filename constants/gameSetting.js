const RANDOM_LENGTH = 3;
const FIRST = 1;
const LAST = 9;
const EXCEPT = 0;
const RESTART = '1';
const EXIT = '2';

module.exports = {
  RANDOM_LENGTH,
  FIRST,
  LAST,
  EXCEPT,
  RESTART,
  EXIT,
};
